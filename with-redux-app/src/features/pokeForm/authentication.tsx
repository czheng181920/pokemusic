import { NextPage } from "next";
//TODO: write the slice file for this (reducers and etc.) and then make this page. this api is already done i think
 
type Repo = {
  access_token: string,
  token_type: string,
  expires_in: number
}
 
const Spotify: NextPage = () => {

  
  return (
    <div>
      {/* Display the fetched data */}
      
    </div>
  )
  
}
 
export default Spotify
